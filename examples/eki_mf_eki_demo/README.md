# EKI and MF-EKI Demo

Runs the EKI vs. MF-EKI demo on a 2D convection-diffusion-reaction model with a lightweight synthetic observation.

## Run

```bash
python example.py
```

## Notes

- Script: `example.py` (duplicate of `docs/source/demos/notebooks/eki_mf_eki_demo.py`).
- Output includes `eki_mf_eki_demo.png` and iteration logs.
